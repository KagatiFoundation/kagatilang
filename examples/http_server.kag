import "std/io";
import "std/socket";
import "std/string";
import "std/process";

def handle_client(fd: integer) -> void {
    let response = "HTTP/1.1 200 OK\r\nContent-Type: text/plain\r\nContent-Length: 12\r\n\r\nHello World!";

    write_socket(fd, response, strlen(response));
    close_socket(fd);
}

def main() -> void {
    let server = create_socket(8080);

    if (server == 0) {
        eprintln("Failed to create socket");
        exit(1);
    }

    listen_socket(server, 10);

    println("Server listening on port 8080");

    loop {
        let client = accept_socket(server);
		println("New client...");

        if (client != 0) {
            handle_client(client);
        }
    }

    close_socket(server);
}