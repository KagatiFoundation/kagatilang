import "std/io";

def check_if_prime(x: integer) -> integer {
	if (x == 3) {
		return 0;
	}

	return -1;
}

def main() -> void {
	let pos = 569;
	let neg = -pos;

	print_int(neg + pos);
	print_int(pos + 1);
	print_int(-neg - 1);
}