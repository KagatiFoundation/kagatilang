import "std/process";
import "std/string";

def main() -> void {
    let pid = execute("ls", "-la");

    if (pid != 1000) {
        let status = wait_process(pid);
        println("Process finished");
    }
}