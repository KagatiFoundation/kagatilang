import "std/time";
import "std/io";

def main() -> integer {
	let a = 11111 + 111;
	let counter = 1 + 1;
	loop if counter < 10 {
		println("in a loop but with time");
		sleep(1);
		counter = counter + 1;
	}
}